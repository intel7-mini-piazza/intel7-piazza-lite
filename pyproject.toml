[project]
name = "intel7-piazza-lite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "khsbl00190-cell", email = "khsbl00190@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "argon2-cffi>=23.1.0",
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "pydantic>=2.8.0",
    "uvicorn[standard]>=0.30.0",
]

[project.scripts]
intel7-piazza-lite = "intel7_piazza_lite:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
